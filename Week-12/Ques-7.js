let listOfstock = ["Shoes", "watch", "cloth", "Cap", "Pant", "Shirt"];

let list_lenght = listOfstock.length;

console.log("List of Shopping List are :");
for (let index = 0; index < list_lenght; index++) {
  console.log(` Item ${index} is ${listOfstock[index]} `);
}
